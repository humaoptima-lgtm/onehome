# ═══════════════════════════════════════
# One Home Smart Engine — Environment Variables
# Copy this file as .env.engine and fill in real values
# ═══════════════════════════════════════

ENV=development
PORT=8000

# Redis
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=onehome_redis_dev

# Database (read-only access, optional)
# DATABASE_URL="postgresql://onehome_user:onehome_pg_dev@postgres:5432/onehome_db"
