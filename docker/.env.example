# ═══════════════════════════════════════
# One Home Docker — Shared Environment Variables
# Copy this file as .env and fill in real values
# ═══════════════════════════════════════

# Redis
REDIS_PASSWORD=onehome_redis_dev

# Meilisearch
MEILI_MASTER_KEY=onehome_meili_dev_key

# PostgreSQL (local dev only — production uses separate DB VPS)
POSTGRES_PASSWORD=onehome_pg_dev
